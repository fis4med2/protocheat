# DCO

Developer Certificate of Origin, version 1.1, applies to all commits. Full text is at https://developercertificate.org/.

By adding this line you certify the contribution is yours to submit:

```
Signed-off-by: fis4med2 <fis4med2@users.noreply.github.com>
```

Use `git commit -s` to add it automatically. The name and email must match your git config and your GitHub account. Earlier seed commits used the alias `fishmed2`; the canonical identity going forward is `fis4med2`.
